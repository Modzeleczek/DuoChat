<Window x:Class="Client.MVVM.View.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:Client.MVVM.View.Converters"
        mc:Ignorable="d"
        Title="{Binding Converter={x:Static c:Strings.Instance}, ConverterParameter='DuoChat - settings'}" Height="300" Width="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/Icons/logo.png"
        FontFamily="/Fonts/RedHatDisplay"
        WindowStartupLocation="CenterOwner"
        MinHeight="100"
        MinWidth="300"
        MouseLeftButtonDown="Window_MouseDown"
        ResizeMode="NoResize">
    <Border Background="#3b3e54"
            CornerRadius="10"
            BorderBrush="#9A8C98"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#22223B" CornerRadius="10,10,0,0">
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Label Content="{Binding Converter={x:Static c:Strings.Instance}, ConverterParameter='DuoChat - settings'}"
                           FontSize="14"
                           Foreground="#9A8C98"
                           FontWeight="SemiBold"
                           Margin="15,0,0,0" />
                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button Style="{StaticResource TransparentStyle}"
                                Width="25"
                                Height="25"
                                Margin="4,1,0,2"
                                Content="🗕"
                                BorderThickness="0"
                                Foreground="#9A8C98"
                                FontWeight="Bold"
                                Click="Button_Minimize_Click"
                                FontSize="15" />
                        <Button
                        Style="{StaticResource TransparentStyle}"
                            Width="25"
                            Height="25"
                            Margin="4,1,16,0"
                            Content="✕"
                            BorderThickness="0"
                            Foreground="#9A8C98"
                            FontWeight="Bold"
                            Click="Button_Close_Click"
                            FontSize="15" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Row="1" BorderThickness="0,2,0,1" BorderBrush="#9A8C98" Margin="0,-1,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                           Content="Nickname:"
                           Foreground="#C9ADA7"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="5,5,5,5"
                               VerticalAlignment="Center"
                               FontFamily="/Fonts/RedHatDisplay" />
                        <Border Grid.Column="1"
                                    BorderBrush="#9A8C98"
                                    BorderThickness="1"
                                    MaxWidth="300"
                                    MinWidth="100"
                                    HorizontalAlignment="Right"
                                    Margin="5,5,10,5"
                                    CornerRadius="10">
                            <TextBox
                                     Background="Transparent"
                                     Margin="3,0,3,0"
                                     MaxWidth="300"
                                     MinWidth="200"
                                     HorizontalAlignment="Right"
                                    BorderThickness="0"
                                    Foreground="White"
                                    VerticalAlignment="Center"
                                    FontFamily="/Fonts/RedHatDisplay"
                                    FontSize="16"
                                    VerticalScrollBarVisibility="Disabled"
                                    HorizontalScrollBarVisibility="Disabled"
                                    CaretBrush="White" />
                        </Border>
                    </Grid>
                </Border>
                <Border Grid.Row="3" BorderThickness="0,1,0,1" BorderBrush="#9A8C98" Margin="0,-1,0,0">
                    <Label
                           Content="Dark theme"
                           Foreground="#C9ADA7"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="5,5,5,5"
                               VerticalAlignment="Center"
                               FontFamily="/Fonts/RedHatDisplay" />
                </Border>
            </Grid>

            <Border Grid.Row="2" Margin="100,5,100,15" BorderBrush="#9A8C98" BorderThickness="1" CornerRadius="10" Padding="1" Width="100" Height="35">
                <Button
                    Content="Save"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Foreground="#C9ADA7"
                    FontSize="18"
                    FontWeight="SemiBold"
                    FontFamily="/Fonts/RedHatDisplay"
                    Style="{StaticResource TransparentStyle}" />
            </Border>
        </Grid>
    </Border>
</Window>