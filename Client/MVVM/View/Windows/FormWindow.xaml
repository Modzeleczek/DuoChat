<windows:DialogWindow 
    x:Class="Client.MVVM.View.Windows.FormWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:c="clr-namespace:Client.MVVM.View.Converters"
                      xmlns:h="clr-namespace:Client.MVVM.View.Helpers"
                      xmlns:windows="clr-namespace:Client.MVVM.View.Windows"
                      xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                      mc:Ignorable="d"
    WindowStyle="None"
                      AllowsTransparency="True"
                      Background="Transparent"
  Height="400" Width="600"
  x:Name="FormWindowName">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding Path=WindowLoaded}"
        CommandParameter="{Binding ElementName=FormWindowName}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Border Background="#4A4E69" BorderBrush="#9A8C98" BorderThickness="2" CornerRadius="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0"
                    Grid.ColumnSpan="2"
                    Background="#22223B"
                    CornerRadius="9,9,0,0"
                    h:EnableDragHelper.EnableDrag="True">
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <Label Content="DuoChat"
                           FontSize="14"
                           Foreground="#9A8C98"
                           FontWeight="SemiBold"
                           Margin="15,0,0,0" />
                    <StackPanel HorizontalAlignment="Right"
                                Orientation="Horizontal">
                       
                    </StackPanel>
                </Grid>
            </Border>
            <StackPanel x:Name="FieldsStackPanel" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" />
            <Border Grid.Row="2" Margin="5,5,5,15" BorderBrush="#9A8C98" BorderThickness="1" CornerRadius="10" Padding="1" Width="100" Height="35">
                <Button Grid.Column="0" x:Name="CancellationButton"
                    Grid.Row="2"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Foreground="#C9ADA7"
                    FontSize="18"
                    Margin="5"
                    FontWeight="SemiBold"
                    FontFamily="/MVVM/View/Fonts/RedHatDisplay"
      Content="{Binding Converter={x:Static c:Strings.Instance},
        ConverterParameter='Cancel'}"
      Command="{Binding Path=Cancel}"
      Style="{StaticResource TransparentStyle}" />
            </Border>
            <Border Grid.Row="2" Grid.Column="1" Margin="5,5,5,15" BorderBrush="#9A8C98" BorderThickness="1" CornerRadius="10" Padding="1" Width="100" Height="35">
                <Button x:Name="ConfirmationButton"
                     
                    Grid.Row="2"
                    Margin="5"
                    Content="{Binding Converter={x:Static c:Strings.Instance},
        ConverterParameter='Confirm'}"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Foreground="#C9ADA7"
                    FontSize="18"
                    FontWeight="SemiBold"
                    FontFamily="/MVVM/View/Fonts/RedHatDisplay"
        Command="{Binding Path=Confirm}"
      Style="{StaticResource TransparentStyle}" />
            </Border>
        </Grid>
    </Border>
</windows:DialogWindow>