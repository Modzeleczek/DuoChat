<UserControl x:Class="Client.MVVM.View.Controls.PreviewablePasswordBox"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  xmlns:local="clr-namespace:Client.MVVM.View.Controls"
  mc:Ignorable="d" 
  d:DesignHeight="450" d:DesignWidth="800"
  x:Name="PreviewablePasswordBoxName">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="auto"/>
    </Grid.ColumnDefinitions>

    <TextBox Grid.Column="0" x:Name="TextBox_" IsReadOnly="True"/>

    <PasswordBox Grid.Column="0" x:Name="PasswordBox_">
      <!--<PasswordBox.Style>
        <Style TargetType="PasswordBox">
          <Setter Property="Visibility" Value="Visible"/>
          <Style.Triggers>
            <DataTrigger Binding="{Binding ElementName=Rectangle_, Path=IsMouseOver}">
              <Setter Property="Visibility" Value="Hidden"/>
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </PasswordBox.Style>-->
    </PasswordBox>

    <Button Grid.Column="1" Click="PreviewPassword_Click" Width="100"/>
  </Grid>
</UserControl>
